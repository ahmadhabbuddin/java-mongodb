# systemLog:
#   destination: file
#   path: "/var/log/mongodb/mongod.log"
#   logAppend: true

storage:
  dbPath: "/data/db"

net:
  port: 27017
  bindIpAll: true

security:
  authorization: enabled
  keyFile: /etc/mongo/mongo-keyfile

replication:
  replSetName: rs0
